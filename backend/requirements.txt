# ============================================================================
# Web Reader Study MVP - Backend Dependencies
# ============================================================================
# 역할: 간 전이 병변 검출을 위한 Reader Study 백엔드 서버 의존성
#
# 설치 방법:
#   cd /home/walehn/ReaderApp
#   python -m venv ReaderApp
#   source ReaderApp/bin/activate
#   pip install -r backend/requirements.txt
#
# 주요 구성:
#   - FastAPI: 웹 프레임워크
#   - nibabel: NIfTI 파일 처리
#   - Pillow: 이미지 렌더링 (PNG 변환)
#   - SQLAlchemy: 데이터베이스 ORM
#   - cachetools: LRU 캐시
#   - bcrypt: 비밀번호 해싱
#   - python-jose: JWT 토큰 처리
# ============================================================================

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6

# NIfTI / Medical Imaging
nibabel>=5.2.0
numpy>=1.24.0

# Image Processing
pillow>=10.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Validation
pydantic>=2.5.0
pydantic-settings>=2.0.0

# Caching
cachetools>=5.3.0

# Authentication
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
